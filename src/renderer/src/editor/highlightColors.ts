/**
 * Fixed highlight palette (no custom colors by design). 200-level pastels so
 * the black note text stays readable on every swatch.
 */
export const HIGHLIGHT_COLORS = [
  '#fef08a', // jaune
  '#bbf7d0', // vert
  '#bfdbfe', // bleu
  '#fbcfe8', // rose
  '#fed7aa' // orange
]
